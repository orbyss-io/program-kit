# W090 Observatory closure evidence

`full-closure-proof.json` is the compact committed receipt for the final
clean-room verification. It records the exact fresh package-root manifest,
three local-publish manifests, five isolated consumers, Release build, unit,
routine conformance, exhaustive gate, and fixture-test observations.

The package and publish binaries remain operation-local evidence below the
recorded temporary proof root. They are deliberately not repository source.
The earlier W065 evidence remains immutable; W090 does not overwrite it.
