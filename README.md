# program-kit

`program-kit/` is reserved for the independently packageable architecture and programming toolkit that will be designed in a later phase.

The Program Kit must remain usable without the Domain Semantic Engine. It must not depend on engine domains, features, or Lab code; the engine may later consume or adapt to the Kit through public contracts. No toolkit architecture or implementation exists during bootstrap.
