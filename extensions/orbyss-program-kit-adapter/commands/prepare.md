---
description: Translate reviewed meaning and request an effect-free Program Kit preparation.
---

Invoke the deterministic adapter `prepare` operation. Preparation may produce
adapter-owned definitions, requests, and an ungranted proposal, but it never
constructs, records authority, or changes product state.

Resolve applicability first from the exact project config at
`.specify/extensions/orbyss-program-kit-adapter/orbyss-program-kit-adapter-config.yml`;
ignore local/environment/ambient layers. Return without profile resolution,
Program Kit launch, or feature write for inactive work. Inherited `assist` with
unresolved applicability is non-blocking. Never initialize Program Kit, record
authority, select a grant, or construct.
