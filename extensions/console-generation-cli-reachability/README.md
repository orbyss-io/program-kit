# Console generation CLI reachability review set

This urgent bounded review set repairs the missing manifest-to-
`DotNetConsoleGenerationInput` binding used by the backed CLI and refresh
journeys.

Canonical review artifacts:

- `architecture-design.md`
- `implementation-plan.md`
- `review-manifest.json`

Implementation must not begin until the human approves the exact design and
plan digests recorded in `review-manifest.json`.
