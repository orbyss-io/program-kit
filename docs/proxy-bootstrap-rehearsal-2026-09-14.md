# Same-session proxy bootstrap rehearsal

The user explicitly extended the completed fictional proxy intake through bootstrap and chose
same-session execution. Run `proxy-9b5215d6a2b3` reached the native first-feature handoff and stopped
with an honest compatibility blocker. It did not complete bootstrap or grant consumer authority.

## Execution and authority

The disposable consumer is
`C:\Users\Joeyb\AppData\Local\Temp\program-kit-intake-qh73a01p`.
The installed native Spec Kit engine executed real shell validators. Its command steps paused for
the current assistant to perform the exact installed producer skill; no independent coding-agent
session or paid-worker authorization was used. This still consumed the current assistant's tokens.

The adapter retained the original intake as `draft`, its proxy marker and scenario bindings.
The original intake SHA-256 remained
`ef7c80f6d9370429e3c8f489efc9f136868344af2c1be21aeb9c8b9deb4f308f`.
Assessment approval and constitution ratification used `approval_mode: simulated-proxy`.
No final bootstrap approval or ordinary bootstrap-completion record exists. Technical checks
were not replaced with simulated successes. The dedicated successful terminal path, tested
deterministically, writes only a separate `rehearsal-completed` result with `authority: none`.

The adapter binds its installed workflow, root, original inputs and response evidence. This is a
provenance guard, not a security boundary against an owner who deliberately edits its implementation.
The workflow definition remained unchanged. The adapter was refreshed during this development
run to repair interpreter/cache setup; this is not immutable-candidate or clean-first-pass evidence.

## Simulated answers and review decisions

The original intake transcript remains the answer record; see the preceding proxy-intake report.
Only the shared-shopping idea came from the user. The assistant supplied the two-partner/equal-rights,
online/private scope and the proposed first outcome. Applicable managed defaults supplied .NET,
Foundation, BFF/local Keycloak, EF/PostgreSQL and the UI profile. No production service, budget,
hosting commitment or real household policy was inferred.

At assessment review, the assistant simulated approval of those exact defaults and the first
record/read outcome, retaining architecture-owned questions about module boundaries, durable state
and membership. At constitution review, it simulated ratification of the written principles on
small outcomes, semantic ownership, private access, durability and reproducible evidence/delivery.
Architecture resolved the three design questions in Proposed ADRs with exact handoff markers.
No further human answer was needed; final acceptance was never presented or simulated.

## Actual progress and checks

| Producer | Terminal result |
|---|---|
| Assessment | 4 checks passed |
| Research | 5 checks passed after schema-runtime setup repair |
| Constitution | Draft validation and exact review-packet generation passed |
| Architecture | 5 structural and 8 final checks passed |
| Tooling | 2 checks passed |
| Roadmap | 3 checks passed; one advisory size target exceeded, no hard-limit failure |
| Closure plan | 3 checks passed |

Native downstream validators and handoffs also ran. These counts identify producer batches, not
independent acceptance trials or a sum of unique assertions.

Three maintained deterministic compatibility cases passed:

- `Managed.dotnet_runtime`: selected SDK builds and executes its minimal program.
- `Managed.browser_runtime`: selected managed Node/Playwright launches Chromium.
- `Managed.foundation_host`: the published image executes its ASP.NET runtime.

The Foundation case first failed because the exact local image was absent. Pulling the unchanged
published `ghcr.io/orbyss-io/foundation-host:v0.2.0` resolved provisioning. Observed registry digest:
`sha256:78d58af0179c58355e969b42f884710ffd305b835fe8c01a1aa2627f0f277866`.
The unchanged recipe then passed; both attempts remain preserved. No image was built or published.
The two already successful proofs were retained rather than reauthored. Chromium evidence is not
the complete product browser matrix; Firefox remains a known local-host limitation and CI duty.

## Roadmap outcome

The portfolio has four entries, not one specification for the entire application:

1. HS-01: record a shared need and see the saved list, with sign-in supporting that outcome.
2. HS-02: mark bought and correct a mistaken mark.
3. HS-03: repeat an available previous item.
4. HS-04: view available purchase history with honest coverage limits.

HS-01 is Blocked. The others remain Candidates with separate future intakes. This demonstrates the
requested decomposition under current-session authoring; it does not prove an independent worker
would make the same choices or use fewer tokens.

## Findings and precise stopping point

The native result is `failed` at `require-first-feature-handoff`:
`FIRST-SLICE-HANDOFF: HS-01 is Blocked; resolve its architectural prerequisites before asking for final acceptance`.

The maintained catalog covers SDK, browser and host runtime checks. It explicitly does not prove
PostgreSQL behavior, consumer shell activation or identity behavior. The proxy architecture retained
`postgres-proof` and `host-profile-proof` as stronger obligations, so no conditional Ready transition
was declared. The handoff correctly stopped instead of allowing document approval to hide them.

These obligations were introduced by the assistant's proxy architecture. The absence of a generic
recipe is not, by itself, a Program Kit defect: custom consumer risks can require custom fixtures.
The unresolved design question is whether these particular checks belong in bootstrap or in first
feature verification. Resolve that boundary explicitly from the selected profile contracts before
either adding bounded compatibility fixtures or moving an obligation to its justified later phase.
Do not relabel them merely to make the rehearsal pass. No product repair or successful end-to-end
bootstrap is claimed from this stop.

Other observed findings:

- Adapter shell execution initially depended on a missing `python` command. The adapter now uses
  the installed workflow interpreter for leading Python shell commands.
- Intake and workflow used different Python versions. Preparation now provisions the existing
  pinned project-local schema runtime for the engine interpreter.
- The inherited UV cache was inaccessible. The adapter scopes UV_CACHE_DIR to its disposable
  project and restores the caller's environment afterward.
- Architecture drafting put API/shell targets in application scope instead of their exact browser
  boundary; the structural gate caught it. Lowercase documentation ID validation caught a second
  authoring error. These were corrected in consumer documents, not by weakening validators.
- Roadmap validation caught omitted ADR evidence for feature/later prerequisite dispositions.
  The existing Proposed decisions were explicitly hash-bound and the batch passed.
- Some tool responses were truncated and needed narrow rereads. This is a real efficiency cost
  of this rehearsal and should not be hidden by a claimed successful producer result.

## Capability changes and verification

The optional proxy-bootstrap command uses the native engine with same-session producer/review
handoffs. Ordinary outer bootstrap remains blocked for proxy intake. Simulated governance evidence
is rejected outside the bound invocation. The adapter retains response history and never grants
feature or release authority. The skill stops at a substantive blocker or proxy completion.

The bounded Development suite passed (49 validators; `artifacts/proxy-bootstrap-development.log`).
After final interpreter/cache and provenance hardening, all 9 targeted proxy tests passed, along
with governance-state positive/negative tests and component/workflow manifest validation. Tests
exercise actual native command/gate pauses and shell failure, draft rejection outside scope,
input tampering, scope restoration, and readiness-dependent proxy-only completion. They start no
coding agent. No release/publication decision was made.

## Evidence and measurement limits

Native run creation to its final handoff stop took approximately 34m46s, including current-session
authoring and pauses; this is not isolated execution time. No reliable per-rehearsal token total or
cost measurement was available from this adapter, so no numerical token saving is claimed.

An ignored local evidence archive preserves 97 files, including fictional outputs, native run state,
contexts, original responses and all four compatibility attempts:
`artifacts/proxy-bootstrap-2026-09-14/evidence.zip`.
SHA-256: `80777c4744680707ba7c99585b64965087829ff3dd5f4cad4b5dcd18cac4f84d`.
`inventory.json` records individual hashes and authority/completion checks. The original disposable
consumer is retained. The archive is local evidence; this tracked report and implementation can be
shared through Git without claiming the archive is included in the push.
