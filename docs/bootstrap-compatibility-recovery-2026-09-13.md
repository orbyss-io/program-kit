# Human consumer compatibility recovery

The stopped native run `ef606c1a` now has four passing, current compatibility
receipts. It has not been resumed, approved or marked complete. No paid agent was
started during this repair. The user repaired their terminal's missing dotnet PATH.

The deterministic recovery helper connects the manual trial to the existing
isolated PostgreSQL service, executes the installed native proof-plan executor,
records redacted process evidence, and removes its database/container/network.
It never invokes a coding agent or workflow engine. It is specific to the four
reviewed knowledge-application trial prerequisites, not a production launcher.

Three missing-input guards were replaced with source-bound tests of published
Foundation 0.2.0, Forms 0.2.0 and CShells preview.147. The corrected Draft selection
includes the JSON and HostedPages mechanisms already required by its founding
design. The original architecture/recipes remain in a recovery snapshot; the
existing Proposed closure decision records the correction for final review.
All eight protected intake/assessment/constitution/baseline hashes are unchanged.

## Executed evidence

- Provider: seven named real PostgreSQL cases cover exact direct-package licenses,
  rollback, concurrent/replayed admission, lost-child-outcome reconciliation,
  lease/restart/recipient fencing, independent-stream ordering and cleanup.
- Published host: actual activation, strict JSON, response headers, generated
  OpenAPI and unchanged-bundle restart, using the registry-resolved Foundation
  v0.2.0 index digest `sha256:78d58af0179c58355e969b42f884710ffd305b835fe8c01a1aa2627f0f277866`.
- Shell boundary: actual two-shell resolution and alternate implementation,
  compiled Core reference inspection and rejection of an injected ASP.NET dependency.
- Forms: real public producer lifecycle, trusted release/tamper rejection,
  published React facade behavior in Chromium/WebKit, and HostedPages HTTP/assets.
  These are synthetic compatibility probes, not full first-slice acceptance.

Final evidence is under
`artifacts/consumer-bootstrap-recovery/9039df1b7f2240d8a39f016ffcb94f93/`, including
`consumer-handoff.json`, exact proof hashes, service operations and cleanup.
Earlier failing and superseded passing receipts were preserved unchanged.
The receipts were renewed after installing the final recovery code: the reuse
guard correctly rejected proofs bound to the earlier executor source.

## Defects repaired during deterministic testing

NuGet cache filenames nested below the compatibility attempt exceeded Windows
MAX_PATH. Dotnet restored them, but the recipe's Python license reader and
MSBuild could not read them; cleanup also failed. Compatibility restores now use
only their own consumer's shorter cache through the same shared restore executor.
Scratch cleanup uses the Windows extended path after verifying its owned boundary.

Nuplane preview.61 local directory feeds extract to `<feed>/.installed`, even
when remote acquisition's PackageInstallRoot is configured. The read-only package
mount therefore needs a separate writable extraction mount. The test and existing
independent acceptance host helper now provide one without changing package bytes.
This was verified against the exact published Nuplane source at
`c292a10390ada26da54865d46820d3ec327c8888`, `MultiFeedPackageResolver.cs`.

The host probe's OpenAPI feed also required a concrete JSON package archive beyond
its framework-provided compile reference; its explicit .NET 10 package pin is
restored/locked by the shared executor. A duplicate global endpoint-name declaration
in the synthetic two-shell fixture was removed. These fixture findings do not imply
that consumer product behavior or full release-bundle packaging has been accepted.

The reusable runtime example now ships once in the governance extension; existing
component-adoption and runtime-bundle references point to applicable evidence and
runtime behavior rather than duplicating another host or resolver.

## Validation and continuation

Bounded Development passed, including native-engine resumption regression.
The scratch/supervisor integration tests passed (5), as did proof-plan/reuse and
long-path cleanup regressions (6). Actual consumer proof execution and a final
read-only `require_proven_closure` check passed against the installed code.

The explicit `--reuse-proven-closure` resume option requires a failed compatibility
shell step and current passing design/source/tooling/contract/test bindings for
every planned probe. It retains the authoring output, resumes at the proof shell,
and preserves downstream validation, final human approval and readiness. It cannot
skip failed/open/stale proofs or mark a workflow complete.

From the user's existing CMD window in the consumer repository:

```cmd
python .specify/extensions/program-kit-governance/scripts/workflow_lifecycle.py resume --run-id ef606c1a --reuse-proven-closure
```

This resumes the existing bootstrap toward its final review. Intake and paid
closure authoring are retained. First vertical-slice delivery remains subsequent
live work; this repair claims neither its execution nor its acceptance.

## Subsequent synchronization recovery

The user resumed `ef606c1a`: the proof shell reused all four receipts and prerequisite
validation passed, then `synchronize-lifecycle` rejected a stale documentation hash
for `bootstrap-prerequisites.json`. Proof closure had changed the ledger after the
architecture map was authored. Lifecycle projection refreshed narrative hashes but
omitted its ledger and acceptance-scope inputs.

Synchronization now validates registered prerequisite evidence and acceptance scope
before refreshing those two document bindings. It continues to reject unrelated
document drift. No proof tooling changed and the four exact receipts remain valid.

Checking the remaining deterministic steps exposed a second mismatch: final bootstrap
validation demanded authenticated web assurance IDs even though assessment explicitly
approved anonymous `none-v1`. The final gate now uses the assurance IDs from the validated,
approved decision register. Authenticated BFF and SPA profiles still require both
versioned web assurance IDs; no consumer approval or security decision was rewritten.

Regression coverage includes stale lifecycle bindings, invalid scope, forged closure
without evidence, unrelated document drift, and complete bootstrap gates for anonymous,
BFF and SPA profiles. Bounded Development passed, supplemented by the final targeted
lifecycle and governance tests. Logs: `artifacts/lifecycle-sync-development.log`,
`artifacts/lifecycle-sync-regression.log`, `artifacts/lifecycle-sync-governance.log`.

Consumer evidence under `artifacts/lifecycle-sync-recovery/` preserves the original
installed helper, map, failed native state, deterministic step output and verified
handoff. Using the actual Spec Kit Python 3.13 interpreter, synchronization, roadmap
projection, consistency, bootstrap validation and final review preparation all pass.
The native workflow state and eight protected authority artifacts remain unchanged;
no coding agent or workflow resume was started by this repair.

The earlier schema failure was an interpreter provisioning gap: Python 3.12's local
schema cache did not provision Spec Kit's Python 3.13 cache. The latter is now set up
with pinned requirements, using a project-local uv cache because the shared cache
was inaccessible. Final consumer verification uses the actual Spec Kit interpreter.

The current failed step is `synchronize-lifecycle`, so resume without the flag that
applies only to a failed proof shell:

```cmd
python .specify/extensions/program-kit-governance/scripts/workflow_lifecycle.py resume --run-id ef606c1a
```

This retains completed stages and proceeds toward the final human bootstrap review.
Readiness and first-slice acceptance are still live work, not results of these checks.
