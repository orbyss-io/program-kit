# ProgramKit.Authentication.SpaPkce

The Program Kit direct SPA-PKCE API profile packaged as one CShells middleware feature. It owns
JWT bearer validation, exact-origin CORS, canonical permission enforcement, and stable
authentication failures. Activate `ProgramKit.Authentication.SpaPkce` in exactly one shell
authentication profile.
