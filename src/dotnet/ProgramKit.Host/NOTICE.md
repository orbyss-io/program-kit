# Attribution

The CShells/Nuplane host composition and eager shell-activation approach are derived from the MIT-licensed
`Elsa.Foundation.Host` project in `elsa-workflows/elsa-foundation`. Program Kit removes Elsa feature and
FastEndpoints dependencies and adds its own immutable bundle bootstrap and configuration contract.
