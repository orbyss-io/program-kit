---
name: publish-dotnet-application-locally
description: Publish one explicitly selected generated .NET host to an explicit local output. Use only when the human supplied the exact host and all five backed command inputs.
---

# publish-dotnet-application-locally

Verify setup and load the complete canonical Program Kit capability by invoking:

`program-kit capabilities preflight publish-dotnet-application-locally --workspace-root .`

`program-kit capabilities read publish-dotnet-application-locally --workspace-root .`

Read and follow the complete returned definition before acting. If either
command is unavailable or reports stale setup, stop and report a Program Kit
setup blocker.

This provider skill is only trigger/registration metadata. The installed
Program Kit CLI owns delivery of the canonical provider-neutral capability.
