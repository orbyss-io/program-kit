# Select build and test

This is an inert supporting completion profile. It is not a capability,
command, trigger, approval, or independently invokable procedure.

Resolve the finite build, analyzer, test, fixture, and verification profiles
that apply to the admitted change. Reuse the repository's backed build and test
operations; do not duplicate compiler, analyzer, restore, or test mechanics.

Run focused checks while developing and every required work-unit or maintenance
gate before completion. Record exact commands, selected profiles, passing
counts, warnings, failures, and checks that could not run.
