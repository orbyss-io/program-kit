namespace GeneratedHost.Composition;

/// <summary>Captured HTTP response from the isolated adapter fixture.</summary>
public sealed record FastEndpointsFixtureResponse(
    int StatusCode,
    string? MediaType,
    string Content);
