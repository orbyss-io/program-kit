# PriceCalculator approved intake, version 1

Fixture ID: `price-calculator-approved-intake`.

This is the exact confirmed intake from the PriceCalculator consumer and the
three original context files referenced by its hashes: project intent,
architecture map and Structurizr workspace. `provenance.json` records the source
run and approved hashes; `scenario.json` records the complete payload inventory.
The payload is self-contained and no longer requires access to the consumer
checkout or a temporary artifact directory. Git line-ending conversion is
disabled for `fixture/` to preserve these byte-level approval bindings.

Use this case for fresh bootstrap completion, a controlled readiness failure,
and workflow-managed resumption to a genuine native engine terminal success.
Its confirmed intake is historical input authority; future architecture and
constitution review gates still require their actual human decisions.

Do not seed recovered architecture decisions, readiness, proofs, approvals or a
completion record. Those are outputs the run must produce. The original consumer
and historical aborted run remain unchanged. Generated evidence belongs under
`artifacts/live-acceptance/`, outside this fixture.
