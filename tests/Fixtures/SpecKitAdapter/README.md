# Spec Kit Adapter fixtures

Fixtures below this directory represent consumer-authored Spec Kit artifacts and
adapter inputs only. Clean-consumer tests must create Program Kit manifests,
locks, adapter registration, handoffs, definitions, requests, grants, and
products through their public production boundaries; those artifacts must not
be copied from this repository as pre-seeded success state.
