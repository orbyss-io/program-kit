# .codex

`.codex/` contains thin Codex-specific wrappers and registration metadata only.

A wrapper loads a canonical definition from `.agents/`; it must not become a second source of the capability's or tool's rules. Do not add other provider wrappers unless a human explicitly requests them.
